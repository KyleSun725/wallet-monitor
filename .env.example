# setup_monitor.py copies this file to .env when no key is entered.
# Only BUZZ_* values are read by the bundled BUZZ adapter.
BUZZ_API_KEY=replace-with-your-key

# Optional settings
BUZZ_BASE_URL=https://buzzai.cc
BUZZ_REFRESH_SECONDS=300
BUZZ_CURRENCY_SYMBOL=$